namespace OneMoreKnight.Flow
{
    /// <summary>
    /// The game's display version (#65). Bump alongside a new entry on the site's
    /// patch-notes page — the two must always tell the same story.
    /// </summary>
    public static class GameVersion
    {
        public const string Current = "alpha 1.3.3";
    }
}
